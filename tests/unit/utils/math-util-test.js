import mathUtil from 'dummy/utils/math-util';
import { module, test } from 'qunit';

module('Unit | Utility | math util');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = mathUtil();
  assert.ok(result);
});
