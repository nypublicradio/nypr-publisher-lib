{{md-text text="
#### `playlist-credit`
Renders a credit line for a music entry. Expects a `music entry` object as its `musicEntry` parameter.

```hbs
{{playlist-credit story=story.playlist.[0]}}
```
"}}

{{playlist-credit musicEntry=model.item.playlist.[0]}}
