{{md-text text="
#### `download-link`
Renders a download link with an icon. Expects a `story` model as its `story` parameter. Doesn't display on iOS devices because the download user experience there is currently poor.

##### Properties
| Property | Default Value | Description|
|--|--|--|
| `story`| | a story
| `icon` | 'download-arrow' | an `nypr-svg` icon name
| `linkText` | 'Download' | the text for the button that toggles the popup
"}}

{{download-link}}
