{{#site-chrome as |chrome|}}

  {{#chrome.nav as |nav|}}
    {{#nav.corner}}
      <h1 class="site-title">{{link-to 'nypr publisher lib''index'}}</h1>
    {{/nav.corner}}
    
    {{#nav.links as |links|}}
      {{links.link text='Models, Adapters, &  Serializers' route='mas'}}
      {{links.link text='Components' route='components'}}
      {{links.link text='Services' route='services'}}
    {{/nav.links}}
  {{/chrome.nav}}
  
  {{#chrome.body}}
    <div class="l-page">
      {{outlet}}
    </div>
  {{/chrome.body}}
{{/site-chrome}}
