{{#if hasBlock}}
  {{yield}}
{{else}}
  {{#each links as |link|}}
    <li class="social-iconlist__item">
      <a class="social-iconlist__link" href={{link.href}} title="{{followPrefix}} {{link.title}}">
        {{~fa-icon link.title~}}
      </a>
    </li>
  {{/each}}
{{/if}}
